export interface Skills {
  icon: string;
  title: string;
  url: string;
}

export interface FooterLink {
  description: string;
  icon: string;
  url: string;
}

export interface NavItem {
  title: string;
  url: string;
}

export interface Projects {
  title: string;
  image: ImageMetadata;
  url: string;
}
