---
import { Icon } from "astro-icon";
import type { CompatibilityItem } from "~/types";

export interface Props {
  items: Array<CompatibilityItem>;
  title: string;
  url: string;
}

const { items, title, url } = Astro.props;
---

<div class="w-full max-w-6xl space-y-2">
  <div class="md:h-36 rounded-lg relative border border-default bg-offset px-4 pt-8 pb-4">
    <!-- <h3
      class="absolute top-0 right-4 -translate-y-1/2 rounded-full border border-current bg-default px-4 py-1 uppercase tracking-tight text-xs"
    >
      {title}
    </h3> -->
    <ul class="grid grid-cols-2 gap-8 sm:grid-cols-3 md:grid-cols-6">
      {
        items.map((item) => (
          <li>
            <div class="flex flex-col items-center gap-2 text-xs">
              <Icon class="h-12" name={item.icon} />
              <span class="text-center">{item.title}</span>
            </div>
          </li>
        ))
      }
    </ul>
  </div>
  <p class="text-right text-sm">
    <!-- <a class="text-primary" href={url}> ...and more &rarr;</a> -->
  </p>
</div>
